services:
  sharrah:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=8000
      - APP_STATE_DB=/app/data/app_state.db
    volumes:
      - ./data:/app/data
